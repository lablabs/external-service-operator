# FIXME:
# change docker helm operator tag after pull request will be releaseds:
# https://github.com/operator-framework/operator-sdk/pull/1818 
FROM quay.io/operator-framework/helm-operator:master

COPY watches.yaml ${HOME}/watches.yaml
COPY helm-charts/ ${HOME}/helm-charts/
